interface:
  display_name: "GitHub Task Flow"
  short_description: "GitHub 이슈부터 단일 커밋 병합까지 안전하게 자동화"
  default_prompt: "Use $github-task-flow to initialize GitHub write access when needed and publish the current workspace changes through verified squash merge."
