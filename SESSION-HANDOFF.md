# 세션 핸드오프

## 현재 기준

- 프로젝트: Canvas 기반 2D 탄성 로프 액션 로그라이크
- 브랜치: `main`
- 현재 단계: 시드 지형과 탄성 로프 이동 1차 프로토타입 구현 및 플레이테스트
- 실행 기반: 브라우저 ES Module, Canvas, Node 기반 로컬 서버와 테스트
- 기술 스택: Ball Fight Simulator와 동일한 Vanilla JavaScript, Alpine.js, Canvas 2D, CSS, Node.js, npm, Prettier, 무번들 구조
- 핵심 문서:
  - `docs/game-hackathon-planning.md`
  - `docs/architecture.md`
  - `docs/development-rules.md`

## 최근 현재 계약 요약

- 로프는 부착 순간 길이를 유지하는 고정 반경 줄이며, 유저의 접선 드래그 충격과 중력으로 진자 운동을 만든다.
- 플레이어는 부착 지점과 해제 타이밍으로 진자 운동을 제어한다.
- 모든 유효한 벽 표면은 정해진 사거리 안에서 부착 가능하다.
- 월드는 시드 기반 완전 절차 생성 방향이다.
- 월드의 주 진행 방향은 아래에서 위이며, 좌우 이동은 진자 운동을 만들기 위한 지그재그 보조축이다.
- 수평 발판은 아래에서 위로 통과하고 위에서 내려올 때만 착지하며, 로프 부착은 양면 모두 허용한다.
- 초기 그래픽은 Canvas 플랫 도형이다.
- 사망하면 해당 런의 아티팩트를 잃는다.
- 첫 공유 프로토타입은 로프 이동과 랜덤 지형을 실제로 플레이할 수 있어야 한다.

## 열린 검증과 다음 작업

1. 실제 플레이로 사거리 440px, 자연 길이 92%, 스프링 강도 46, 방사 감쇠 0의 손맛을 조정한다.
2. 부착 유지 중 스윙과 해제 직후 관성의 연속 화면 증거를 추가 확보한다.
3. 절차 생성 경로의 장거리 통과 가능성을 자동 검사한다.
4. 로프와 월드 검증 뒤 적 1종과 아티팩트 2~3종을 추가한다.

## 활성 결정

### [L1] 2026-08-10 — 월드의 주 진행축은 아래에서 위로 고정한다

- 맥락: 옆으로 긴 지형은 달리기와 점프에 유리해 로프가 핵심 이동 수단이 되지 못했다.
- 결정: 런은 월드 아래에서 시작해 정상으로 올라간다. 경로 발판은 3개 수직 레인 사이를 지그재그로 배치하고 점프 높이보다 크게, 로프 사거리보다 가깝게 잇는다.
- 영향: `WorldGenerator`는 48단계 수직 경로, 측면 스윙 벽, 천장형 앵커를 생성하며 HUD는 현재 등반 높이를 표시한다.
- 검증: `tests/worldGenerator.mjs`의 단계별 상승·사거리 계약과 실제 Canvas 시작 화면

### [L2] 2026-08-10 — GitHub Pages는 main 브랜치 루트에서 직접 게시한다

- 맥락: 현재 프로젝트는 별도 빌드 산출물이 없는 정적 ES Module·Canvas 환경이며, GitHub Actions 배포는 준비와 실행 대기 시간이 추가된다.
- 결정: GitHub Actions 기반 Pages 배포를 제거하고 저장소 설정에서 `main` 브랜치의 `/(root)`를 게시 소스로 사용한다. 루트 `index.html`을 사이트 진입점으로 삼는다.
- 영향: `main`에 병합된 정적 파일이 별도 커스텀 workflow 없이 더 빠르고 단순한 경로로 배포된다. 빌드 단계가 필요해질 때만 Actions 방식 재도입을 검토한다.
- 검증: GitHub Pages 저장소 설정과 배포된 루트 `index.html` 확인

### [L1] 2026-08-10 — 싱글도 공용 권한 시뮬레이션을 사용한다

- 맥락: PvE 협동을 추가할 때 싱글과 멀티의 게임 규칙과 월드 구현이 갈라지면 유지보수와 판정 일관성이 깨진다.
- 결정: 입력을 불변 `PlayerCommand`로 만들고 `LocalAuthority`가 공용 `GameSimulation`을 실행한다. 싱글은 플레이어 1명인 동일 시뮬레이션으로 취급한다.
- 영향: Canvas와 입력기는 상태를 직접 변경하지 않으며, 향후 네트워크는 명령과 스냅샷 전송 계층만 교체한다.
- 검증: 동일 시드와 240틱의 동일 명령이 같은 플레이어 상태를 만드는 재생 테스트를 추가했다.

### [L2] 2026-08-10 — 고정 길이 로프에서 접선 충격과 중력으로 회전한다

- 맥락: 탄성 로프와 지속 A/D 가속은 유저가 방향을 정하지 않아도 운동을 만들어 아크샨식 조작 의도와 달랐다.
- 결정: 부착 순간의 로프 길이를 고정하고 방사 속도를 제거하되 중력의 접선 성분은 유지한다. 방향 드래그가 임계점을 넘을 때만 부착당 한 번 620의 접선 충격을 준다.
- 영향: 부착 자체와 A/D 홀드는 가속을 만들지 않으며, 이후 운동은 충격·중력·관성과 해제 타이밍으로 결정된다.
- 검증: 고정 반경, 방사 속도 제거, 접선 속도 보존, 지속 방향 입력 무효화를 자동 테스트한다.

### [L2] 2026-08-10 — 부착 후 접선 드래그로 한 번의 능동 스윙을 제공한다

- 맥락: 해제 타이밍만으로는 산나비처럼 사용자가 즉시 운동을 만드는 액션감이 부족했다.
- 결정: 부착 후 로프에 수직인 방향으로 44px 드래그하면 드래그한 접선 방향으로 460의 임펄스를 한 번 적용한다. 로프 방향 성분은 발동 판정에서 제외한다.
- 영향: 별도 대시 키 없이 부착·드래그·해제의 마우스 동작으로 스윙을 완성하며, 재사용하려면 로프를 다시 부착해야 한다.
- 검증: 접선 투영 방향, 방사 드래그 무시, 임펄스의 직교 속도 보존을 자동 테스트한다.

### [L2] 2026-08-10 — 첫 로프 물리는 약한 탄성을 가진 진자 줄로 튜닝한다

- 맥락: 해제 타이밍만으로 진자 운동을 조작하려면 부착 직후 로프가 늘어난 상태여야 한다.
- 결정: 최대 사거리 440px, 부착 거리의 78% 자연 길이, 초당 70px 자동 회수, 스프링 강도 46, 방사 감쇠 0으로 시작한다. 부착 중 A/D 입력이 약한 수평 힘을 가하며 입력 없는 최저속 보정은 사용하지 않는다.
- 영향: 수치는 `src/game/config.js` 한곳에서 조정하며 로프 입력은 마우스 홀드·해제로 유지한다.
- 검증: `tests/elasticRope.mjs`와 실제 Canvas 플레이테스트

### [L1] 2026-08-10 — Ball Fight Simulator와 같은 브라우저 네이티브 기술 스택을 사용한다

- 맥락: 팀원이 구현 환경과 UI·Canvas 책임 경계를 같은 기준으로 이해해야 한다.
- 결정: Vanilla JavaScript ES Module, Alpine.js 3.x, Canvas 2D, CSS, Node.js 20 이상, npm, Prettier를 사용하고 번들러를 두지 않는다.
- 영향: Alpine.js는 DOM UI만 담당하고 게임 로직과 Canvas 렌더링은 독립 모듈로 유지한다. TensorFlow.js처럼 원본 게임의 기능 전용 의존성은 자동으로 가져오지 않는다.
- 검증: `docs/tech-stack.md`, `.prettierrc`, `package.json`의 포맷 명령

### [L1] 2026-08-09 — 로프 숙련과 아티팩트 빌드를 핵심 경험으로 둔다

- 맥락: 액션 손맛과 로그라이크 성장 중 무엇을 중심에 둘지 결정해야 했다.
- 결정: 순간 플레이는 로프 숙련이 주도하고, 아티팩트는 공격 방식과 성장 폭을 만든다.
- 영향: 자동 공격 빌드도 로프 이동과 생존을 대체할 수 없다.
- 검증: 로프 프로토타입 플레이테스트 예정

### [L1] 2026-08-09 — 월드는 시드 기반 완전 절차 생성으로 시작한다

- 맥락: 해커톤 콘텐츠 제작 비용을 줄이고 반복 플레이 변화를 확보해야 했다.
- 결정: 발판과 벽을 알고리즘으로 생성하고 같은 시드는 같은 월드를 만든다.
- 영향: 통과 가능성 검증과 실패 시드 재현이 필수다.
- 검증: 월드 생성 테스트 예정

### [L1] 2026-08-09 — 공용 기반과 게임 규칙을 분리한다

- 맥락: Ball Fight Simulator의 기반을 참고하되 게임별 가정을 가져오면 안 된다.
- 결정: `game-kit`은 순수 기반만 소유하고 로프·월드·적·아티팩트는 `src/game/`이 소유한다.
- 영향: import 경계 테스트를 유지한다.
- 검증: `tests/gameKitBoundary.mjs`

### [L2] 2026-08-09 — 초기 비주얼은 Canvas 플랫 도형으로 만든다

- 맥락: 에셋 제작보다 로프 움직임과 VFX 가독성을 빠르게 검증해야 했다.
- 결정: 지형, 플레이어, 로프, 목표를 단순한 형태와 색으로 표현한다.
- 영향: 픽셀아트와 완성형 애니메이션은 후순위다.
- 검증: 실제 Canvas 화면 검증 예정

## 갱신 규칙

- 현재 구현과 다음 작업에 영향을 주는 결정만 이 문서에 유지한다.
- 완전히 흡수되거나 대체된 결정은 `docs/decision-history.md`로 이동한다.
- 결정은 삭제하지 않고 대체 관계와 이유를 남긴다.
