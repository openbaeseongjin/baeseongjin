# Final Escape Cinematic

The completed-run presentation uses the supplied MP4. The supplied GIF is the browser fallback when video playback cannot begin. `FinalEscapeCinematic` starts once per app instance only after the authoritative `runState` becomes `completed`; it does not create or replicate a separate cinematic event.

Boss06 boarding authority remains unchanged: all active players board directly, then the existing server-owned completion transition replicates `runState: completed` to every client.

| File | SHA-256 |
| --- | --- |
| `one_rope_final_escape_original_image_16x9.mp4` | `0065465b9ae9456748124d293c363d7cdd89700b19ee86334474122b13fd2472` |
| `one_rope_final_escape_with_english_credits_preview.gif` | `b9a1100c099edfd206eed3c67c907e9b16b927b5be243bd7b79d946cf0a2acad` |
