# Runtime effect assets

투사체와 VFX의 검증된 runtime package를 `<effect-id>/`에 둔다. 아직 공개 manifest·loader·validator 계약은 없으며, 계약이 생기기 전에는 [`assets/artwork/effects/`](../../artwork/README.md)에만 납품한다.

효과의 판정과 수명주기는 gameplay가 소유하며 그래픽 package는 표현 자료만 가진다.
