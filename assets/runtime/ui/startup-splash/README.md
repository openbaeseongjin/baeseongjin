# Startup splash runtime asset

`one-rope-splash.png`는 `assets/artwork/ui/startup-splash/export/one-rope-splash.png`의 무변형 runtime 사본이다. `index.html`이 이 고정 경로를 시작 오버레이에서 직접 사용하며 원본 비율을 유지한다. 이미지 실패 시에도 검은 시작 배경과 CSS 로더는 유지된다.
