# 5-5 VALIDATION

Manual/runtime:
- Base-clear key-low route
- Base-clear key-high route
- ascent inaccessible before proof
- priority-directive unlocks ascent
- return loop avoids full retrace
- Jammer never blocks both first key anchors
- AEGIS kill optional
- mobile Hub shot reads key vs locked destination

After integration:
`npm run validate:area-specs`
`npm run check`
`npm test`
