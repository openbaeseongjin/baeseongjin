# Meeting minutes

Dated files in this directory are generated by the Discord meeting bot after `/meeting end`. Because this repository and `docs/` GitHub Pages site are public, GitHub publication is blocked by default until the team explicitly enables `ALLOW_PUBLIC_GITHUB_MINUTES=true`.
